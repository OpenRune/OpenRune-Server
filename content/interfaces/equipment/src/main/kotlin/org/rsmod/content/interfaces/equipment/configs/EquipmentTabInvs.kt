package org.rsmod.content.interfaces.equipment.configs

import dev.openrune.inv

internal typealias equip_invs = EquipmentTabInvs

object EquipmentTabInvs {
    val death_data = inv("inv.diango_hols_sack")
    val death = inv("inv.deathkeep_items")
    val kept = inv("inv.skill_guide_hunting_tracking")
}
