package org.rsmod.api.net.central.netty

internal object WorldLinkHandlerNames {
    const val READ_TIMEOUT = "worldlink-read-timeout"
    const val LENGTH_FRAME_DECODER = "worldlink-length-frame-decoder"
    const val LENGTH_FRAME_ENCODER = "worldlink-length-frame-encoder"
    const val INBOUND_PACKET_DECODER = "worldlink-inbound-packet-decoder"
    const val INBOUND_QUEUE = "worldlink-inbound-queue"
}
